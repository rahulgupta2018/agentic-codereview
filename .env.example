# Environment Configuration for Code Review System
# Copy this file to .env and fill in your values

#==============================================================================
# Environment Selection
#==============================================================================
# Options: development, staging, production
ENVIRONMENT=development

#==============================================================================
# Session Database Configuration
#==============================================================================

# DEVELOPMENT (SQLite - Default)
# Automatically used if SESSION_DB_URI is not set
# Location: data/sessions/sessions.db

# STAGING (PostgreSQL on Docker)
# Uncomment to use Docker PostgreSQL:
# SESSION_DB_URI=postgresql://adk_user:devpassword@localhost:5432/adk_sessions

# PRODUCTION (Cloud SQL - Google Cloud)
# SESSION_DB_URI=postgresql://adk_user:PRODUCTION_PASSWORD@/adk_sessions?host=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME

# PRODUCTION (RDS - AWS)
# SESSION_DB_URI=postgresql://adk_user:PRODUCTION_PASSWORD@adk-sessions.xxxxx.us-east-1.rds.amazonaws.com:5432/adk_sessions?sslmode=require

# PRODUCTION (Azure Database for PostgreSQL)
# SESSION_DB_URI=postgresql://adk_user@adk-sessions:PRODUCTION_PASSWORD@adk-sessions.postgres.database.azure.com:5432/adk_sessions?sslmode=require

#==============================================================================
# Database Credentials (for Docker Compose)
#==============================================================================
DB_PASSWORD=devpassword
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin

#==============================================================================
# LLM API Keys
#==============================================================================

# Gemini (Google)
GEMINI_API_KEY=AIzaSyDG1WH01KTYXHkKvSDE0Ps-B7Edtv1hTbU
GEMINI_MODEL=gemini-2.5-flash

# Ollama Configuration (for local development)
OLLAMA_API_BASE=http://localhost:11434
OLLAMA_MODEL=ollama_chat/granite4:latest
OLLAMA_SUBAGENT_MODEL=ollama_chat/granite4:latest

#==============================================================================
# GitHub Integration
#==============================================================================
GITHUB_TOKEN=your_github_personal_access_token
GITHUB_WEBHOOK_SECRET=your_webhook_secret

#==============================================================================
# ADK API Server Configuration
#==============================================================================
API_HOST=0.0.0.0
API_PORT=8000

# Enable cloud tracing (for production)
TRACE_TO_CLOUD=false

# Enable OpenTelemetry to cloud (for production)
OTEL_TO_CLOUD=false

#==============================================================================
# Google Cloud Configuration (Production)
#==============================================================================
GCP_PROJECT_ID=your-gcp-project-id
GCP_REGION=us-central1

# Cloud SQL Instance Connection Name
# Format: PROJECT_ID:REGION:INSTANCE_NAME
CLOUD_SQL_INSTANCE=your-project:us-central1:adk-sessions

#==============================================================================
# AWS Configuration (Production)
#==============================================================================
AWS_REGION=us-east-1
AWS_RDS_ENDPOINT=adk-sessions.xxxxx.us-east-1.rds.amazonaws.com

#==============================================================================
# Logging & Monitoring
#==============================================================================
LOG_LEVEL=INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL

#==============================================================================
# Security
#==============================================================================
# JWT Secret for API authentication (if implementing)
# JWT_SECRET=your_jwt_secret_here

# CORS Origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,https://your-domain.com
